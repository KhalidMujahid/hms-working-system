<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Observation Entry</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 min-h-screen p-6">
    <div class="max-w-4xl mx-auto bg-white p-6 rounded shadow">
      <h1 class="text-2xl font-bold mb-4 text-gray-700">
        New Observation Entry
      </h1>

      <div class="mb-4 text-gray-600">
        <p>
          <strong>Patient:</strong> <%= patient.firstName %> <%=
          patient.lastName %>
        </p>
        <p><strong>ID:</strong> <%= patient._id %></p>
      </div>

      <form
        action="/doctor/observations/new/<%= patient._id %>"
        method="POST"
        class="space-y-4"
      >
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div>
            <label class="block font-medium">Date</label>
            <input
              type="date"
              name="date"
              class="w-full mt-1 border px-3 py-2 rounded"
              required
            />
          </div>

          <div>
            <label class="block font-medium">Time</label>
            <input
              type="time"
              name="time"
              class="w-full mt-1 border px-3 py-2 rounded"
              required
            />
          </div>

          <div>
            <label class="block font-medium">Temperature (°C)</label>
            <input
              type="number"
              step="0.1"
              name="temperature"
              class="w-full mt-1 border px-3 py-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium">Pulse (bpm)</label>
            <input
              type="number"
              name="pulse"
              class="w-full mt-1 border px-3 py-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium">Respiration Rate</label>
            <input
              type="number"
              name="respirationRate"
              class="w-full mt-1 border px-3 py-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium"
              >Blood Pressure (Systolic / Diastolic)</label
            >
            <div class="flex space-x-2">
              <input
                type="number"
                name="systolic"
                placeholder="Systolic"
                class="w-1/2 border px-3 py-2 rounded"
              />
              <input
                type="number"
                name="diastolic"
                placeholder="Diastolic"
                class="w-1/2 border px-3 py-2 rounded"
              />
            </div>
          </div>

          <div>
            <label class="block font-medium">Fluids Intake</label>
            <input
              type="text"
              name="fluidsIntake"
              class="w-full mt-1 border px-3 py-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium">Fluids Output</label>
            <input
              type="text"
              name="fluidsOutput"
              class="w-full mt-1 border px-3 py-2 rounded"
            />
          </div>
        </div>

        <div>
          <label class="block font-medium">Remark</label>
          <textarea
            name="remark"
            rows="3"
            class="w-full mt-1 border px-3 py-2 rounded"
          ></textarea>
        </div>

        <div class="border-t pt-4 mt-6">
          <h2 class="text-lg font-semibold mb-2">Drugs and Infusion</h2>
          <div class="space-y-4">
            <div class="bg-gray-50 p-4 rounded shadow-sm">
              <div>
                <input
                  type="text"
                  name="drugName"
                  placeholder="Dose"
                  class="w-full mt-1 border px-3 py-2 rounded"
                />
              </div>
            </div>
          </div>
        </div>

        <button
          type="submit"
          class="mt-6 bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700"
        >
          Submit Observation
        </button>
      </form>
    </div>
  </body>
</html>
