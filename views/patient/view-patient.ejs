<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Patient</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">
    <div class="bg-white shadow-lg rounded-lg p-8 max-w-3xl w-full">
      <h1 class="text-2xl font-bold mb-6 text-center text-gray-800">
        Patient Details
      </h1>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >First Name:</label
          >
          <p class="text-gray-900"><%= patient.firstName %></p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Last Name:</label
          >
          <p class="text-gray-900"><%= patient.lastName %></p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Email:</label
          >
          <p class="text-gray-900"><%= patient.email %></p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Phone:</label
          >
          <p class="text-gray-900"><%= patient.phone %></p>
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-semibold text-gray-700"
            >Address:</label
          >
          <p class="text-gray-900">
            <%= patient.address.street || 'N/A' %>,<%= patient.address.city ||
            'N/A' %>,<%= patient.address.state || 'N/A' %>
          </p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Gender:</label
          >
          <p class="text-gray-900"><%= patient.gender %></p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Date of Birth:</label
          >
          <p class="text-gray-900">
            <%= patient.dateOfBirth ? patient.dateOfBirth.toDateString() : 'N/A'
            %>
          </p>
        </div>
        <div>
          <label class="block text-sm font-semibold text-gray-700"
            >Blood Type:</label
          >
          <p class="text-gray-900"><%= patient.bloodType || 'N/A' %></p>
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-semibold text-gray-700"
            >Allergies:</label
          >
          <p class="text-gray-900">
            <%= patient.allergies.length !== 0 ? patient.allergies : 'None' %>
          </p>
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-semibold text-gray-700"
            >Medical History:</label
          >
          <p class="text-gray-900">
            <%= patient.medicalHistory.length !== 0 ? patient.medicalHistory :
            'No history' %>
          </p>
        </div>
        <div class="md:col-span-2">
          <label class="block text-sm font-semibold text-gray-700"
            >Emergency Contact:</label
          >
          <p class="text-gray-900">
            <%= patient.emergencyContact.name || 'N/A' %>, <%=
            patient.emergencyContact.relation || 'N/A' %>, <%=
            patient.emergencyContact.phone || 'N/A' %>
          </p>
        </div>
      </div>

      <div class="mt-6 text-center">
        <a href="/doctor/appointments" class="text-blue-600 hover:underline">
          &larr; Back to Appointments
        </a>
      </div>
    </div>
  </body>
</html>
