<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Vitals | EHRMS</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
  </head>
  <body class="flex bg-gray-100 min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-green-700 text-white flex flex-col p-6 space-y-4">
      <h2 class="text-2xl font-bold text-center mb-6">Nurse</h2>
      <a
        href="/nurse/dashboard"
        class="hover:bg-green-600 px-3 py-2 rounded flex items-center gap-2"
        ><i class="fas fa-home"></i> Dashboard</a
      >
      <a
        href="/nurse/vitals/list"
        class="hover:bg-green-600 px-3 py-2 rounded flex items-center gap-2"
        ><i class="fas fa-list"></i> Vitals List</a
      >
      <a
        href="/auth/logout"
        class="hover:bg-red-600 bg-red-500 px-3 py-2 rounded flex items-center gap-2 mt-auto"
        ><i class="fas fa-sign-out-alt"></i> Logout</a
      >
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-6">
      <div class="max-w-2xl mx-auto bg-white p-6 rounded shadow">
        <h1
          class="text-2xl font-semibold text-gray-800 mb-6 flex items-center gap-2"
        >
          <i class="fas fa-edit text-green-700"></i> Edit Vitals for <%=
          vital.patient_id.firstName %> <%= vital.patient_id.lastName %>
        </h1>

        <form
          action="/nurse/vitals/edit/<%= vital._id %>"
          method="POST"
          class="space-y-4"
        >
          <div>
            <label
              class="block font-medium text-gray-700 mb-1"
              for="temperature"
              >Temperature (°C)</label
            >
            <input
              type="number"
              step="0.1"
              name="temperature"
              id="temperature"
              value="<%= vital.temperature %>"
              required
              class="w-full border border-gray-300 p-2 rounded"
            />
          </div>

          <div>
            <label
              class="block font-medium text-gray-700 mb-1"
              for="blood_pressure"
              >Blood Pressure (mmHg)</label
            >
            <input
              type="text"
              name="blood_pressure"
              id="blood_pressure"
              value="<%= vital.blood_pressure %>"
              required
              class="w-full border border-gray-300 p-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium text-gray-700 mb-1" for="pulse"
              >Pulse Rate (bpm)</label
            >
            <input
              type="number"
              name="pulse"
              id="pulse"
              value="<%= vital.pulse %>"
              required
              class="w-full border border-gray-300 p-2 rounded"
            />
          </div>

          <div>
            <label
              class="block font-medium text-gray-700 mb-1"
              for="respiratory_rate"
              >Respiratory Rate (breaths/min)</label
            >
            <input
              type="number"
              name="respiratory_rate"
              id="respiratory_rate"
              value="<%= vital.respiratory_rate %>"
              required
              class="w-full border border-gray-300 p-2 rounded"
            />
          </div>

          <div>
            <label class="block font-medium text-gray-700 mb-1" for="notes"
              >Notes (optional)</label
            >
            <textarea
              name="notes"
              id="notes"
              rows="3"
              class="w-full border border-gray-300 p-2 rounded"
            >
<%= vital.notes %></textarea
            >
          </div>

          <div class="flex items-center justify-between mt-6">
            <a
              href="/nurse/vitals/list"
              class="text-gray-600 hover:underline flex items-center gap-1"
            >
              <i class="fas fa-arrow-left"></i> Back to List
            </a>
            <button
              type="submit"
              class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 flex items-center gap-2"
            >
              <i class="fas fa-save"></i> Update Vitals
            </button>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
