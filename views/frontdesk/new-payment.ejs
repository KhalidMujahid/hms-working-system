<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Payment</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-gray-100 min-h-screen"
  >
    <div
      class="max-w-2xl mx-auto mt-12 bg-white dark:bg-gray-800 p-8 rounded-lg shadow-lg transition-all"
    >
      <h1 class="text-2xl font-bold mb-6 text-gray-700 dark:text-gray-100">
        💳 Add New Payment
      </h1>

      <form method="POST" action="/frontdesk/payments/new" class="space-y-6">
        <!-- Patient -->
        <div>
          <label for="patient" class="block text-sm font-medium mb-1"
            >Patient</label
          >
          <select
            id="patient"
            name="patient"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-gray-50 dark:bg-gray-700"
          >
            <% patients.forEach(p => { %>
            <option value="<%= p._id %>">
              <%= p.firstName %> <%= p.lastName %> - <%= p.phone %>
            </option>
            <% }) %>
          </select>
        </div>

        <!-- Amount -->
        <div>
          <label for="amount" class="block text-sm font-medium mb-1"
            >Amount (₦)</label
          >
          <input
            id="amount"
            type="number"
            name="amount"
            required
            step="0.01"
            placeholder="Enter amount"
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-gray-50 dark:bg-gray-700"
          />
        </div>

        <div>
          <label for="method" class="block text-sm font-medium mb-1"
            >Payment Method</label
          >
          <select
            id="method"
            name="method"
            required
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-gray-50 dark:bg-gray-700"
          >
            <option value="cash">Cash</option>
            <option value="card">Card</option>
            <option value="transfer">Bank Transfer</option>
            <option value="insurance">Insurance</option>
          </select>
        </div>

        <div>
          <label for="purpose" class="block text-sm font-medium mb-1"
            >Purpose</label
          >
          <input
            id="purpose"
            type="text"
            name="purpose"
            required
            placeholder="e.g., Consultation, Medication"
            class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-gray-50 dark:bg-gray-700"
          />
        </div>

        <div class="pt-4">
          <button
            type="submit"
            class="flex items-center gap-2 bg-green-600 hover:bg-green-700 text-white font-medium px-6 py-2.5 rounded-lg transition-colors duration-200"
          >
            <i class="fas fa-save"></i>
            Save Payment
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
