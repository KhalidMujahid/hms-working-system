<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Family Care Hospital - Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <style>
      .floating-label input:focus + label,
      .floating-label input:not(:placeholder-shown) + label {
        transform: scale(0.85) translateY(-1.5rem);
        color: #2563eb; /* blue-600 */
      }
    </style>
  </head>
  <body
    class="bg-gradient-to-br from-blue-300 via-blue-500 to-blue-700 min-h-screen flex items-center justify-center px-4"
  >
    <div
      class="bg-white/80 backdrop-blur-md p-10 rounded-2xl shadow-2xl w-full max-w-md border border-white/30"
    >
      <div class="text-center mb-8">
        <h2 class="text-3xl font-bold text-blue-800 drop-shadow-sm">
          <i class="fas fa-user-md text-blue-600 mr-2"></i>HMS Login
        </h2>
        <p class="text-sm text-gray-600">Family Care Hospital</p>
      </div>

      <form method="POST" action="/login" class="space-y-6">
        <div class="relative floating-label">
          <input
            type="email"
            name="email"
            id="email"
            required
            placeholder=" "
            class="peer w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-white/60 backdrop-blur-sm placeholder-transparent text-gray-800"
          />
          <label
            for="email"
            class="absolute left-4 top-3 text-gray-500 transition-all peer-placeholder-shown:top-3.5 peer-placeholder-shown:text-base peer-focus:top-2 peer-focus:text-sm"
            >Email Address</label
          >
        </div>

        <div class="relative floating-label">
          <input
            type="password"
            name="password"
            id="password"
            required
            placeholder=" "
            class="peer w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-white/60 backdrop-blur-sm placeholder-transparent text-gray-800"
          />
          <label
            for="password"
            class="absolute left-4 top-3 text-gray-500 transition-all peer-placeholder-shown:top-3.5 peer-placeholder-shown:text-base peer-focus:top-2 peer-focus:text-sm"
            >Password</label
          >
        </div>

        <% if (error) { %>
        <div class="text-red-600 text-sm text-center"><%= error %></div>
        <% } %>

        <button
          type="submit"
          class="w-full bg-white text-blue-700 font-semibold border border-blue-600 py-3 rounded-xl hover:bg-blue-600 hover:text-white transition duration-300 shadow-md"
        >
          <i class="fas fa-sign-in-alt mr-2"></i>Login
        </button>
      </form>

      <div class="mt-6 text-center text-sm text-gray-600">
        © 2025 Family Care Hospital
      </div>
    </div>
  </body>
</html>
